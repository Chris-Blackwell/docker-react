language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t cdblackwell/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true cdblackwell/docker-react npm run test